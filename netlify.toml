[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — mirrors vercel.json so client-side routes resolve to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
